/*
  Warnings:

  - You are about to drop the column `skipQuestions` on the `round_privileges` table. All the data in the column will be lost.

*/
-- AlterTable
ALTER TABLE `round_privileges` DROP COLUMN `skipQuestions`;
